[pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
markers =
    slow: tests that import TensorFlow / hit heavy dependencies
filterwarnings =
    ignore::DeprecationWarning

